#include <stdio.h>

int main(){
    int lado;
    printf("Digite o lado do quadrado");
    scanf("%d",lado);
    int area = lado*lado;
    printf("Area do quadrado eh %d",area);
    return 0;
}